# npm 10.x arborist crashes on this peer graph ("edgesOut") in default
# peer-resolution mode; the build only needs devDeps present (the shipped
# artifact resolves peers from the host), so resolve the legacy way.
legacy-peer-deps=true
